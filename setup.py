from setuptools import setup

"""
setup.py is used with pyproject.toml and setup.cfg to configure and install
SQL Antipattern Scanner package.

pyproject.toml:
    Specifies build system requirements and backend for project.
    Declares use of setuptools for building package.

setup.cfg:
    Contains package metadata, dependencies, and other configuration options.
    Includes details like package name, version, author, description, and more.

Uses setup() from setuptools to read configuration from setup.cfg and set up package.
"""
setup()